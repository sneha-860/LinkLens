/**
 * Fix simulation (edge added to a graph copy, PageRank recomputed) and ranking by S(u→v) = ΔPR_v × σ_hybrid(u,v) / κ(u), with rule-based explanations and orphan rescue donors.
 *
 * Not implemented yet.
 */
export {};
